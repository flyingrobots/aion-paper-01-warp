# LaTeX build artifacts
*.aux
*.bbl
*.blg
*.log
*.out
*.toc
*.synctex.gz
*.fls
*.fdb_latexmk
*.pdf

# Keep the final PDF if desired, but usually build artifacts are ignored.
# Un-comment below to track the specific output PDF if needed, though git is bad at binary blobs.
!pdf/aion-rmg.pdf
!paper/figures/*.pdf
*.pdf
